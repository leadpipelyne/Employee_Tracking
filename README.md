# Data Directory

Place the following files here before running the calculation:

1. **SalarySheet.xlsx** — The monthly salary sheet containing the `Salaries` and `Leaves` tabs.
2. **LeaveRequests.xlsx** — The leave requests file containing approved leave data.

These files contain sensitive salary information and are excluded from version control via `.gitignore`.
